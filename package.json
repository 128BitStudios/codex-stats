{
  "name": "codex-stats-updated",
  "author": "128bitstudios",
  "repository": {
    "type": "git",
    "url": "https://github.com/128bitstudios/codex-stats"
  },
  "displayName": "Codex Stats (Updated)",
  "description": "Monitor your ChatGPT/Codex Stats and rate limits",
  "version": "1.0.5",
  "publisher": "128bitstudios",
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": [
    "Visualization"
  ],
  "icon": "resources/blossom.dark.png",
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "icons": {
      "codex-blossom": {
        "description": "Codex Blossom Icon",
        "default": {
          "fontPath": "resources/codex-icon.woff",
          "fontCharacter": "\\e900"
        }
      }
    },
    "commands": [
      {
        "command": "codex-usage.refresh",
        "title": "Refresh Codex Stats"
      },
      {
        "command": "codex-usage.login",
        "title": "Login to Codex"
      }
    ],
    "configuration": {
      "title": "Codex Stats",
      "properties": {
        "codexUsage.updateInterval": {
          "type": "number",
          "default": 300,
          "description": "Update interval in seconds (default: 300 = 5 minutes)"
        },
        "codexUsage.showNotifications": {
          "type": "boolean",
          "default": false,
          "description": "Show notifications when rate limits are high"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "vscode:publish": "vsce publish",
    "build": "npm run compile",
    "compile": "tsc -p ./",
    "vscode:package": "vsce package",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile",
    "lint": "eslint src --ext ts",
    "test": "node --test \"test/**/*.js\""
  },
  "devDependencies": {
    "@types/node": "16.x",
    "@types/vscode": "^1.74.0",
    "@typescript-eslint/eslint-plugin": "^5.45.0",
    "@typescript-eslint/parser": "^5.45.0",
    "@vscode/vsce": "^3.9.2",
    "eslint": "^8.28.0",
    "typescript": "^4.9.3"
  },
  "dependencies": {
    "axios": "^1.6.0"
  }
}
